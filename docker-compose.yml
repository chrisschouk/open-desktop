version: '3.8'

services:
  # OpenOrgo Orchestrator & API Server
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: openorgo-server
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - VISION_API_ENDPOINT=${VISION_API_ENDPOINT:-https://api.openai.com/v1/chat/completions}
      - VISION_MODEL=${VISION_MODEL:-gpt-4o-mini}
      - VISION_API_KEY=${VISION_API_KEY:-}
    restart: unless-stopped

  # Default Computer Sandbox Node Container
  sandbox-node-1:
    build:
      context: ./sandbox-engine
      dockerfile: Dockerfile.sandbox
    container_name: openorgo-sandbox-1
    ports:
      - "6080:6080" # noVNC WebSocket Stream
      - "8000:8000" # Internal Agent Control Daemon
    environment:
      - SCREEN_WIDTH=1280
      - SCREEN_HEIGHT=800
    restart: unless-stopped

  # Frontend Dashboard Web App
  dashboard:
    image: nginx:alpine
    container_name: openorgo-dashboard
    ports:
      - "80:80"
    volumes:
      - ./client:/usr/share/nginx/html:ro
    restart: unless-stopped
